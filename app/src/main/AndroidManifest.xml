<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.careerhub">

    <!-- ✅ Permissions MUST be here -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- OPTIONAL: Only for Android 9 and below -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.CareerHub">

        <activity android:name=".NotificationSettingsActivity" />
        <activity android:name=".ResumeMenuActivity" />
        <activity android:name=".CgpaActivity" />
        <activity android:name=".JobsActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ResumePreviewActivity" />
        <activity android:name=".ResumeFormActivity" />
        <activity android:name=".SubjectEntryActivity" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
